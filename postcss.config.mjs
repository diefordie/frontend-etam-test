// Mengimpor plugin TailwindCSS
import tailwindcss from 'tailwindcss';

// Konfigurasi PostCSS
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

export default config;
